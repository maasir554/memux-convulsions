{
  "name": "plasma",
  "private": true,
  "version": "0.1.0",
  "description": "Local-first chat harness with an OpenAI-compatible backend that adapts to local AI servers (AMD Lemonade first).",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run dev",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "typecheck": "npm --workspace backend run typecheck && npm --workspace frontend run typecheck"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
